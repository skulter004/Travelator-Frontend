export interface TravelRequest {
    id: string,
    name: string;        
    travelType: string;  
    purpose: string;     
    cost: number;        
    startDate: string;   
    endDate: string;  
  }